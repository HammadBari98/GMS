@model IEnumerable<Gym_Management.Models.Gym_Branch>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout3.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.trainee.trainee_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gym_Branch_1.B_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.trainee.trainee_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gym_Branch_1.B_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GB_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.GB_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GB_id })
        </td>
    </tr>
}

</table>
